# 遇到不明确的条件第一时间停止任务询问用户

> **核心规则**：执行任务时遇到不明确的条件，**立即停止**当前执行路径，向用户提问。禁止自行猜测、静默降级、继续产出可能不可信的结果。

## 触发条件（遇到以下任一情况立即停止）

| # | 场景 | 判定标准 |
|---|------|---------|
| 1 | **工具/能力不可用或加载状态未确认** | 被要求使用的工具（SKILL/MCP/API）调用失败；或执行依赖某个工具已加载但未经验证 |
| 2 | **资源未归档/不可达** | 被要求引用的资源（URL/文件/证据）无法访问，或未在预期位置 |
| 3 | **执行路径有多种合理解读** | 提示词/指令中存在歧义，或当前场景未被明确规则覆盖 |
| 4 | **输出与预期格式存在偏差** | 产出与预期 schema/格式不一致，且无明确规则说明如何处理 |
| 5 | **前置条件未验证** | 执行依赖某个前置状态（如"SKILL 已加载""双盲密封"），但该状态未经验证 |

## 执行动作

立即停止当前任务执行，向用户提问，包含三要素：

1. **明确指出**遇到的不明确条件（具体到哪一条规则/哪个字段/哪个工具）
2. **列出可能的处理方式**（最多 3 个选项，含影响说明）
3. **说明**每种方式对任务结果的影响（如"会导致 R3 反证不可达，结论置信度降一档"）

**禁止行为**：
- ❌ 自行猜测后继续执行
- ❌ 静默降级（如换工具、换 URL、换格式）后继续执行
- ❌ 继续执行产出可能不可信的结果

## 历史教训（基于真实事件，须避免重蹈）

### 教训 1：SKILL 未加载事件（2026-06-25）

**场景**：执行 search-orchestrator 相关任务时，从未确认 SKILL 是否被 Cline 加载，长期按"通用调研方法论"执行。

**后果**：Run #1~#9c 所有实验结论可信度存疑，需回顾评估；浪费多轮实验资源。

**正确做法**：第一次执行前应停止询问："SKILL 是否已加载？如何验证？验证失败是否继续？"

### 教训 2：search 工具不可用静默降级（2026-06-25 Run #10）

**场景**：DDG search 返回 `fetch failed`，模型自行改用 `fetch_content` 直接抓取已知 URL，未告知用户。

**后果**：R3 反证型 query 全部不可达，所有 sub-Q 标注 `❌ [未找到反证]`，结论置信度降一档；实验有效性受损。

**正确做法**：应停止询问："search 工具不可用，是否继续？降级到 fetch_content 是否影响实验有效性？R3 不可达如何处理？"

### 教训 3：跨语言抽取规则不明确（2026-06-25 Run #10）

**场景**：P6 Highlights 要求 verbatim 抽取，但英文原文 → 中文总结是否允许未明确。模型自行归纳，产出 1 条 paraphrase。

**后果**：Extractive Fidelity 从 96% 降到 92.3%，评分从 5/5 降到 4/5。

**正确做法**：应停止询问："跨语言场景下 verbatim 规则如何适用？英文原文是否允许中文归纳？"

### 教训 4：归档范围不明确 → 已明确为 Iron Law（2026-06-25 Run #10 + Run #11）

**场景**：Run #10 §2 "fetch_content 全文归档" 只存了摘要非完整正文；Q2-4 引用的 pgbench.html 未在 §2 归档中出现。Run #11 同样问题——§2 只存摘要，导致 baseline SimHash 在 3-8 verbatim 对上 Miss（"摘要级指纹"≠"文档级指纹"），Net Gain +0.80 成为上界估计。

**后果**：Run #10 保真度检查时无法对 Q2-4 做完整字符串匹配验证；Run #11 baseline 真实 verbatim 检测能力被低估，Net Gain 为上界估计。

**规则已明确**：SKILL.md §2.1 已新增 Iron Law——"每个 fetch_content 成功的 URL，其返回正文必须完整归档到输出文件的独立章节"。不再是"不明确条件"，而是须遵守的硬性规则。§3.6.1 P6 触发条件已补澄清——"不进合成 context"≠"全文丢弃"。

**正确做法**：执行 Phase 2 fetch_content 后，每个成功 URL 的完整正文必须归档到输出文件 §2。摘要 ≠ 全文归档。

## 与项目规则的关系

- 本规则是 [docs/project-rules.md](docs/project-rules.md) 防漂移约束的**行为层补充**：项目规则约束文档职责划分，本规则约束模型执行行为
- 本规则不替代 SKILL 流程，仅在执行 SKILL 流程中遇到不明确条件时触发停止
- 本规则优先级高于"完成任务"——宁可停止询问，不可静默降级产出不可信结果
